spring.application.name=ecomm

server.port=7855

spring.datasource.driver-class-name=${DATABASE_DRIVER:org.postgresql.Driver}
spring.datasource.platform=${DATABASE_PLATFORM:postgres}
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://localhost:5432/ecom}
spring.datasource.username=${DATABASE_USER:postgres}
spring.datasource.password=${DATABASE_PASSWORD:Vishal5592}
spring.jpa.properties.hibernate.dialect = ${SPRING_JPA_PROPERTIES_HIBERNATE_DIALECT:org.hibernate.dialect.PostgreSQLDialect}

spring.jpa.properties.hibernate.show_sql=true

spring.jpa.hibernate.ddl-auto=create